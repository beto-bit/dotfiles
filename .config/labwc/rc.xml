<?xml version="1.0"?>

<!--
  This is a very simple config file with many options missing. For a complete
  set of options with comments, see docs/rc.xml.all
-->

<labwc_config>
  <libinput>
    <device category="default">
      <naturalScroll>yes</naturalScroll>
    </device>
  </libinput>

  <theme>
    <name></name>
    <cornerRadius>8</cornerRadius>
    <font name="sans" size="10" />
  </theme>

  <keyboard>
    <default />
    <numlock>true</numlock>

    <keybind key="W-q">
      <action name="Execute" command="kitty" />
    </keybind>

    <keybind key="W-e">
      <action name="Execute" command="index" />
    </keybind>

    <keybind key="W-r">
      <action name="Execute" command="rofi -show drun -show-icons" />
    </keybind>

    <keybind key="Print">
      <action name="Execute">
        <command>sh -c 'grim -g "$(slurp -d)" - | wl-copy'</command>
      </action>
    </keybind>

    <keybind key="XF86_PowerOff">
      <action name="Execute" command="wlogout" />
    </keybind>

  </keyboard>

  <mouse>
    <default />
    <!-- Show a custom menu on desktop right click -->
    <context name="Root">
      <mousebind button="Right" action="Press">
        <action name="ShowMenu" menu="some-custom-menu" />
      </mousebind>
    </context>
  </mouse>

</labwc_config>
